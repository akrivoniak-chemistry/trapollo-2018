{% extends "base.twig" %}

{% block content %}
	<section class="default-page post-type-{{post.post_type}}" id="post-{{post.ID}}">
		<h1 class="heading-1 c-orange mt60 tac">{{post.title}}</h1>
		<div class="span-8 push-2 span-12@sm mt50 lh1.5">
			{{post.content}}
		</div>
	</section>
{% endblock %}